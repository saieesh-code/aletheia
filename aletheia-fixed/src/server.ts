/**
 * server.ts — Legacy SSR entry point (not used in SPA mode).
 *
 * This file is retained for reference only. The app was migrated from
 * TanStack Start (SSR) to a pure Vite SPA served by the Rust backend.
 * main.tsx is the actual entry point; this file is never imported.
 *
 * The error-capture and error-page utilities are still used by the root
 * route error boundary.
 */
export {};
