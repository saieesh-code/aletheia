/**
 * example.functions.ts — Legacy TanStack Start server function example.
 *
 * This file is not used. The app uses a pure Vite SPA with a Rust backend.
 * Server functions (createServerFn) are not available in SPA mode.
 * Retained as documentation of the original pattern only.
 */
export {};
